services:
  - type: web
    name: motor-qr-system
    runtime: python
    plan: free
    buildCommand: bash deploy.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
